<li>
	{{burger_name}}

	<button class="change-devour btn btn-danger btn-sm" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}Devour!{{else}}Order Again{{/if}}
	</button>

	<button class="delete-burger btn btn-dark btn-sm" data-id="{{this.id}}">DESTROY</button>
</li>